# 会话交接：功能3 完成并推送 + 下一步

> **用途**：新对话快速恢复上下文。开场先读本文件 + `CLAUDE.md` + `重构任务.md`。
> **日期**：2026-06-13（功能3 已推送）

---

## 一句话现状

**功能3（对话持久化 + 召回）已完成、端到端 6 项验证全过、已分 4 个 commit 推送到 GitHub `origin/dev`。** 本地与远程完全同步。下一步两选一：① 定那 3 个遗留判断题；② 进入功能2（多模态存储，需先 brainstorm + 写 spec）。

---

## 协作约定（重要，务必遵守）

- **git 写操作（add/commit/push）一律由用户手动执行**，我只给命令、不替他跑。
- **文档不进 git**：`CLAUDE.md`、`重构任务.md`、`docs/` 都在 `.gitignore` 里，是本地文档。
- **学习导向**：用户通过重构学设计模式，每个设计决策要讲 WHY + 权衡，不只给代码。
- **用户质疑 ≠ 反对**：第一次问"为什么"别急着翻盘，先解释。
- **回复用中文**。
- 改代码前先确认授权；本项目无测试框架（`GLOB_RECURSE` 会让测试 `main()` 符号冲突），验证靠"编译通过 + 端到端手动测"。

---

## 功能3 已推送的内容（origin/dev，11 个文件 / 4 个 commit）

提交链：`e8efd76`(链 sqlite3) → `f42b468`(ConversationStore) → `8a88e70`(工具：文本召回) → `e60c7f3`(USS 接入持久化与组合根装配)。

**新增文件（3）：**
- `src/Persistence/ConversationStore.{h,cpp}` — SQLite 存储适配器。`append`/`loadAll`/`search`/`clearUser`/`removeLast`。db 开失败降级纯内存 + LOG_ERROR；预处理语句防注入；WAL 模式；自身不加锁（靠 USS 的 `mutex_message`）。`search` 已加 `id DESC LIMIT 30` 再升序重排。
- `src/Tool/ToolContext.h` — 结构体承载调用期 `user_id`（结构体而非裸参 = 开闭原则，功能2 加 group_id 不动签名）。
- `src/Tool/RecallConversationTool.h` — header-only，持 `ConversationStore&`，关键词 LIKE 检索拼文本回灌模型。

**修改文件（8）：**
- `CMakeLists.txt` — 链接 `sqlite3`；修正悬空 include `src/Database`→`src/Persistence`。
- `src/Tool/Tool.h` — `execute(args)` → `execute(args, const ToolContext&)`。
- `src/Tool/GetTimeTool.h` — 跟随基类改签名（忽略 ctx）。
- `src/ChatService/ChatService.cpp` — tool 调用点传 `ToolContext{user_id}`；写回从"读整段→push→覆盖"改增量 `appendMessage`。
- `src/UserSession/UserSessionService.{h,cpp}` — 注入 `ConversationStore&`；新增 `appendMessage`（锁内 内存 push + INSERT）；`ensureUserExistsUnlock` 懒加载；reset/removePreviousContext 同步删库；**公共 `ensureUserExists` 改为委托 `ensureUserExistsUnlock`**（终审修复，否则 admin 启动 seed 内存/库分叉）。
- `src/main.cpp` — 组合根创建 `ConversationStore`（路径 `configVariableOpt("CONVERSATION_DB_PATH","source/conversations.db")`）注入 USS、注册 `RecallConversationTool`。

**铁律延续**：tool 回合只活在 `bundle.request.history` 临时变量，**绝不落库**（表里只有 user/assistant，无 tool），避免孤儿 tool_call 重载触发 API 400。

**终审（opus）修复的两处**：① 公共 `ensureUserExists` 未懒加载 → 已改委托 Unlock。② `search()` 无 LIMIT → 已改 `id DESC LIMIT 30` 升序重排。

**端到端验证（kimi-k2.6 实测，全过）**：落盘✅ / tool 回合不落库✅ / 重启不丢（懒加载，测了两次重启）✅ / 召回触发判断（在上下文拒调、缺上下文主动调）✅ / 召回检索闭环（"名字"未命中→换"埃芒"命中）✅ / reset 同步清库（12→2 行、人格保留）✅。

---

## 下一步 1：功能3 遗留判断题（用户定，3 项未决）

1. `UserSessionService::updateChatHistory` + `getChatHistory` 改增量写后**已无调用者**。保留（文档化未用接口）vs YAGNI 删除？
2. **人格未持久化**：`Person.system_prompt` 纯内存，重启后对话从库回来但自定义人格（设置人格）丢回默认。要不要把人格也持久化？（可作功能3 follow-up）
3. 召回是**单行 LIKE 命中**，非话题片段召回。要不要扩成"命中行 ± N 邻行"？（spec 设计是靠模型换词重试兜底，目前够用）

---

## 下一步 2：功能2 多模态存储（未开始，需先 brainstorm + 写 spec）

路线图（`重构任务.md`）：功能1 tool use ✅ → **功能2 多模态** → 功能3 持久化 ✅（功能3 已插队先做完）。

功能2 范围（`conversations` 表本期保持纯文本，功能2 才动图片）：
- 识图原图 / 生图产物落盘 + `get_image` 工具取回。
- 绘图/看图改为 LLM 自主调用的 tool（`generate_image` / 替代现有 `#图片生成:` 显式命令和 Vision 意图路由）。
- 真正的新设计点：`ChatService::reply` 返回值 `string` → `vector<OutboundMessage>`（文本+图片混合序列）。
- **reset 清图坑**：功能2 后 reset 须连图片元数据行 + 磁盘文件一起清（删库行 + 删文件无法原子，单机做法 = 尽力而为 + 失败记日志）。
- 启动前用 `superpowers:brainstorming` 探需求，再写 spec / plan。

---

## 关键技术备忘（这次对话学到的）

- **缓存命中 = 前缀逐字 token 匹配**（prefix cache），非语义匹配。功能3 的三个设计恰好对缓存友好：增量 append（前缀稳定）、时间戳只进库不进 system prompt、召回结果不落库（下轮前缀不被污染）。**不能靠 description 反复询问来命中缓存**。想再榨缓存只能换支持显式 `cache_control` 的 API（如 Anthropic），属 LLMPort 契约层大改，现在不值得。
- `OpenAIStandard.cpp:78-87` 有个 400 重试：收到 400 时 strip 掉 temperature/penalty 重发（kimi 拒绝 temperature>1）。**用户说"400 那个先不管"**，与功能无关。
- sqlite3 CLI **未装**，查库用 `python3 -c "import sqlite3..."`。
- 测试时改过 `config.json` 的 `MESSAGE_SURVIVAL_TIME`（86400→60 制造过期历史测召回），**已还原为 86400**。
- 验证残留：`build/source/conversations.db*` 可能还有测试数据，要清可 `rm -f build/source/conversations.db*`（db 已被 gitignore，不影响 git）。

## 下一步 3：召回增强（2026-06-13 讨论定论，新对话来实现）

> 本节是一次完整讨论的结论沉淀。背景：debug 模式下发现上下文发的是**原文**而非 description 压缩，引出"description 摘要方案到底要不要做"的复盘。

### 先厘清当前机制（确认无误）

- **窗口永不爆炸的原理**：内存存**全量**（`appendMessage` 无限 push，从不删），但 `buildChatRequest`（`UserSessionService.cpp:153-202`）发送时**只裁近 20 条 + 过滤超存活期**，原始历史只读不回写。所以发送侧有界 → token 成本**线性于 n，非二次**（撞到 20 条天花板就平了）。
- **超 20 条的老对话**：只靠 SQLite 全量兜底。模型在当前上下文找不到时，自己调 `recall_conversation(keyword)` 去**库**里检索回灌（recall 走 `store.search()` 查库，**不读内存**）。
- **召回的命门**：`search` 是字面 `LIKE '%keyword%'`，**只能命中字面含该词的行**。当初说"养了只**猫**叫煤球"→ 搜"猫"能中；但"**煤球**今天拆家"（无"猫"字）→ 搜"猫"**永远捞不到**。召回质量 = 模型回调时给的关键词质量。这是字面检索的天花板。

### 结论 1：description 摘要方案 —— **正式淘汰**（不是推迟）

复盘后判定 description（每条对话回滚生成摘要、LLM 看压缩版）**应淘汰，不再作为 follow-up 挂着**。三个维度全被更优解接管：

1. **省 token 维度**：description 是"常驻摘要"（每轮都在上下文里，非零常数成本）；现状 slicing 是"老对话直接丢出窗口 = 0 token + recall 按需捞回"。**丢弃+按需 比 常驻摘要 更省**——description 在它最该赢的维度反而输。
2. **找回旧对话维度**：recall 工具已接住（更便宜）。
3. **语义召回维度**（description 唯一能赢字面 recall 的场景，如"猫↔煤球"）：被 **embedding 向量检索全面压制**（更准、不需每条多调一次 LLM、不砸 prefix cache、原文无损）。
4. **业务动向**：模型上下文窗口在变大（128k~200k+ 且还在涨），"压缩省 token"前提随时间贬值。真要扩窗口直接调大 `MAX_TURNS` 即可，零成本。

→ description **两头不到岸，没有独占价值区间**。真有语义召回需求，路线是 **recall → 检索升级**，绝不会回头搞每条摘要。

**额外成本（也是淘汰理由）**：每轮多一次 LLM 调用（延迟翻倍 + 钱），且"近 N 原文 + 更早摘要"会让对话滑进摘要区时**前缀变化 → 砸 prefix cache**（缓存命中 = 前缀逐字 token 匹配，见本文档"关键技术备忘"）。

### 结论 2：召回增强方案选型（受众是普通用户，**零额外配置**是硬约束）

embedding 向量检索（RAG 的检索核心）虽然技术上最优，但**需要专用 embedding 模型**（chat 模型只吐字不吐向量）。普通用户连 `ModelsName.json` 都难配，再让他们找 embedding 模型 = 劝退。**故 embedding 方案对本项目否决**（受众约束，非技术问题）。

零配置、不依赖额外模型的备选（按改动小→大）：

- **方案 A：chat 模型扩词（首选先试）**。`recall_conversation` 收到"猫"后，先让**已有的 chat 模型**生成同义/相关词 `["猫","煤球","宠物","橘猫"...]`，用这批词一起 `LIKE ... OR ...`。本质=词表扩展逼近语义匹配。零新依赖、零配置、用户无感，只动 `recall` 内部。代价：多一次 chat 调用；对**自创专有名词**（如"煤球"这种昵称）仍可能漏，因为模型生成的是通用同义词，不知道用户具体说过啥。
- **方案 B：本地分词 + 倒排（cppjieba）**。入库分词建倒排，检索按词召回 + 命中词数排序（弱化版 BM25）。纯本地、零模型、零联网、对用户透明。代价：加一个分词**库**依赖（编译进去，非"模型"，用户无感）；仍是词面，解决不了无共同词的语义鸿沟（猫↔煤球仍漏）。
- **方案 C：模型打标签（搁置）**。入库时让 chat 模型提 1~3 个实体标签存库，检索匹配标签。是 description 极简版，但**仍每条多一次 chat 调用**——撞 description 同款墙，否。

**推荐路径**：先上 **方案 A**（性价比最高，只动 recall 内部）；不够再上 **方案 B**；C 和 embedding 均搁置。

### 实现前必做的一件事（重要）

**当前字面 LIKE 的实际漏检率未被验证过。** 在加任何增强前，先实测：模型"换词重试"是不是大多数时候能捞到？

- 若实测够用 → 连方案 A 都不急，避免过早优化。
- 若实测撞见明确漏检 case → 针对该 case 选方案（很可能 A 就够）。

→ **新对话开工第一步：先用一段时间 / 构造几个 case 实测漏检，再决定上不上、上哪个。**

### 顺带记录：内存全量存储的小遗留（低优先级）

内存 `user_chatHistory` 全量存储，但**超过 20 条的部分实际无人读**（发送只取近 20，recall 查库不查内存）。当前个人 Bot 规模（几百条 = 几百 KB）**不疼，可不动**。规模大了（重度用户几万条）再做"高水位/低水位滚动删除"：如保留上限 40、超了砍回 30（批量滚动，非每条 erase）。**坑**：保留量要给"过期过滤"留余量，不能贴着 20 砍（否则过滤后发送不足 20 条）。改动会动 `appendMessage`，是真代码改动，做前单独评估。

---

## 相关文档
- 设计 spec：`docs/superpowers/specs/2026-06-12-conversation-persistence-design.md`
- 实现计划：`docs/superpowers/plans/2026-06-13-conversation-persistence.md`（8 任务，全完成）
- 功能1 设计：`docs/superpowers/specs/2026-06-12-llm-tool-use-design.md`
